function isOldEnoughToDrive(age) {
    let mayorEdad;
        if (age >=16) {
            mayorEdad = true;
        }
        else {
            mayorEdad = false;
        }
    console.log (mayorEdad);
}
isOldEnoughToDrive (22);