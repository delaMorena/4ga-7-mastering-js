var output = isOldEnoughToDrink(22)
function isOldEnoughToDrink (age) {
    if (age >= 21) {
        return true;
    } else { 
        return false;
    }
    // your code here	
}
console.log (output)
